# Flask Document Extraction Application

This application is a Flask-based web application that utilizes LangChain and OpenAI's GPT-4o model to extract relevant details from specified document types. It stores extraction fields and descriptions in a SQL database and provides a user-friendly interface for data input and extraction.

## Features

- Upload document context and extraction fields.
- Utilize OpenAI's GPT-4o for data extraction based on provided guidelines.
- Store and retrieve extraction fields and descriptions using SQLAlchemy.
- Tokenize input text to handle OpenAI's token limit.

## Prerequisites

- Use requirements file to install dependencies
- pip install -r requirements.txt code to install dependencies

